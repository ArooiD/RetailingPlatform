package ru.mirea.sdk.api;

class StorageSender extends AbstractModuleSender implements ModuleSender {
    public StorageSender(String storage, String modulePath) {
        super(storage, modulePath);
    }
}
