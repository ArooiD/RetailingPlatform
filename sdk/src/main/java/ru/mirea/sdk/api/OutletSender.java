package ru.mirea.sdk.api;

class OutletSender extends AbstractModuleSender implements ModuleSender{
    public OutletSender(String outlet, String modulePath) {
        super(outlet, modulePath);
    }
}
